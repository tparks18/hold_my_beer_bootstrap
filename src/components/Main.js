import React from 'react'
import Navbar from './Navbar';
import Footer from './Footer';

const Main = () => {
    return <React.Fragment>
        <header>
        <Navbar />
        </header>

        <main>

        </main>

        <footer>
        <Footer />
        </footer>
        </React.Fragment>;
}

export default Main
